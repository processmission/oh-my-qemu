interface:
  display_name: "QEMU Source Provenance"
  short_description: "Record QEMU source and artifact provenance."
  default_prompt: "Use $qemu-source-provenance to record the sources, toolchains, outputs, and hashes for this QEMU boot task."
