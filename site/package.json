{
  "name": "oh-my-qemu-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "rm -rf dist .astro node_modules/.astro && astro build",
    "check": "npm run build"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.18",
    "astro": "^7.0.2"
  }
}
